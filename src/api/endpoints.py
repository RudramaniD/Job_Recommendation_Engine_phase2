import logging\nfrom typing import Dict, Any, Optional\nfrom fastapi import FastAPI, HTTPException, status\nfrom pydantic import BaseModel, Field\n\nfrom ..engine.qdrant_recommendation_engine import QdrantRecommendationEngine\nfrom config.settings import *\n\n# Configure logging\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\napp = FastAPI(\n    title=\"Job Recommendation Engine\",\n    description=\"AI-powered job matching system using Qdrant VectorDB\",\n    version=\"1.0.0\"\n)\n\n# Initialize engine\nconfig = {\n    \"MONGO_URI\": MONGO_URI,\n    \"DB_NAME\": DB_NAME,\n    \"JOBS_COLLECTION\": JOBS_COLLECTION,\n    \"EMBEDDING_MODEL\": EMBEDDING_MODEL,\n    \"QDRANT_HOST\": \"localhost\",\n    \"QDRANT_PORT\": 6333,\n    \"QDRANT_COLLECTION\": \"job_embeddings\",\n}\n\ntry:\n    engine = QdrantRecommendationEngine(config)\nexcept Exception as e:\n    logger.error(f\"Failed to initialize recommendation engine: {e}\")\n    raise\n\nclass CandidateRequest(BaseModel):\n    candidate_id: str = Field(..., min_length=1, max_length=100)\n    headline: Optional[str] = Field(None, max_length=500)\n    desired_title: Optional[str] = Field(None, max_length=200)\n    skills: Optional[str] = Field(None, max_length=2000)\n    summary: Optional[str] = Field(None, max_length=2000)\n    experience: Optional[str] = Field(None, max_length=2000)\n    city: Optional[str] = Field(None, max_length=100)\n    province: Optional[str] = Field(None, max_length=100)\n    country: Optional[str] = Field(None, max_length=100)\n\nclass FiltersRequest(BaseModel):\n    work_setting: Optional[list] = Field(None, max_items=10)\n    job_type: Optional[list] = Field(None, max_items=10)\n    experience_levels: Optional[list] = Field(None, max_items=10)\n\nclass JobMatchRequest(BaseModel):\n    candidate: CandidateRequest\n    filters: Optional[FiltersRequest] = None\n\n@app.get(\"/health\")\nasync def health_check():\n    \"\"\"Health check endpoint\"\"\"\n    return {\"status\": \"healthy\", \"service\": \"job-recommendation-engine\"}\n\n@app.post(\"/match-jobs\")\nasync def match_jobs(request: JobMatchRequest):\n    \"\"\"Match jobs for a candidate\"\"\"\n    try:\n        candidate_dict = request.candidate.dict(exclude_none=True)\n        filters_dict = request.filters.dict(exclude_none=True) if request.filters else None\n        \n        result = engine.match_jobs_for_candidate(candidate_dict, filters_dict)\n        return result\n        \n    except ValueError as e:\n        logger.warning(f\"Invalid input: {e}\")\n        raise HTTPException(\n            status_code=status.HTTP_400_BAD_REQUEST,\n            detail=str(e)\n        )\n    except Exception as e:\n        logger.error(f\"Job matching failed: {e}\")\n        raise HTTPException(\n            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,\n            detail=\"Internal server error\"\n        )\n\n@app.get(\"/stats\")\nasync def get_stats():\n    \"\"\"Get VectorDB statistics\"\"\"\n    try:\n        stats = engine.get_vectordb_stats()\n        return stats\n    except Exception as e:\n        logger.error(f\"Failed to get stats: {e}\")\n        raise HTTPException(\n            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,\n            detail=\"Failed to retrieve statistics\"\n        )\n\n@app.post(\"/initialize\")\nasync def initialize_vectordb():\n    \"\"\"Initialize Qdrant collection\"\"\"\n    try:\n        engine.initialize_vectordb()\n        return {\"message\": \"VectorDB initialized successfully\"}\n    except Exception as e:\n        logger.error(f\"VectorDB initialization failed: {e}\")\n        raise HTTPException(\n            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,\n            detail=\"VectorDB initialization failed\"\n        )\n\n@app.post(\"/migrate\")\nasync def migrate_jobs():\n    \"\"\"Migrate jobs from MongoDB to Qdrant\"\"\"\n    try:\n        engine.migrate_jobs_to_vectordb()\n        return {\"message\": \"Jobs migrated successfully\"}\n    except Exception as e:\n        logger.error(f\"Migration failed: {e}\")\n        raise HTTPException(\n            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,\n            detail=\"Migration failed\"\n        )\n